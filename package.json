{
  "name": "fujiyoshi",
  "version": "1.0.0",
  "description": "*既存の管理方法に大体合わせていただければ大丈夫です*",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:sass",
    "build:sass": "sass src/sass/style.sass dist/recruit-site/assets/css/style.css --style=expanded --source-map",
    "watch": "npm run watch:sass",
    "watch:sass": "sass src/sass/style.sass dist/recruit-site/assets/css/style.css --style=expanded --source-map --watch",
    "dev": "concurrently \"npm run watch:sass\" \"live-server dist --port=3000\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2",
    "sass": "^1.69.0"
  }
}
